namespace MyShop.Infrastructure.Repositories;

public static class RepositoryExtensions
{
    //public static int Merge<TEnity>(this IRepository<TEnity> repository, IEnumerable<TEnity> entities)
    //{
    //    int count = 0;
    //    foreach (var entity in entities) 
    //    {
    //        var item = repository.Get(entity);
    //    }
    //    return count;
    //}
}